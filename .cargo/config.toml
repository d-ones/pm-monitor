[target.xtensa-esp32s2-none-elf]
runner = "espflash flash --chip esp32s2 --no-stub --monitor"

[env]

[build]
rustflags = [
  "-C", "link-arg=-nostartfiles",
]

target = "xtensa-esp32s2-none-elf"

[unstable]
build-std = ["core", "alloc"]
